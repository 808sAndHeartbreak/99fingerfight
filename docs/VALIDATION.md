# 验证记录 · 2026-09-09

本轮重排战场与精简演出，规则版本 4，客户端协议版本 2。

- 85 项自动测试通过，生产构建成功。包含真实双 WebSocket 完整对局、技能与道具、防御、重连、名字、重复命令和超时。
- 新增未命名双方的席位名称、改名后重连、进入对战前保存草稿、规划后立即计算的回归测试。
- 两个真实浏览器验证：青岚／赤焰填写后直接创建／加入，双方画面名称一致；准备开局、结束规划、选择手与触碰完成，操作权正常交接。
- 教学实际验证：任一对手手可点击；道具取消后重新使用；合成后立即攻击；界面资源无控制台错误。
- 底部图鉴常驻，鼠标滚轮与原生横向滚动支持。道具移至手外侧，三个坑位，头部不重复显示。5 的防御信息随数字更新。

限制：未做真实手机触屏、并发压力或公网弱网全覆盖。Three.js 延迟加载包约 529 KB，有构建体积提示，不影响构建成功。

本轮新增三阶段显示、零回合特写等待、道具结果提示不额外锁定操作，以及对手道具悬停数值归属和生命/背包上限测试。

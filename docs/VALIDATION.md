# 当前版本验证记录（规则 15 / 协议 2）

以下为当前规则的定向验收，旧规则验收记录已移出当前文档，可在 Git 历史中追溯。测试数字对应记录当次执行，不代表每次发布都会执行全量测试。

## 道具规则与图鉴稳定性 · 2026-09-13 · 规则 15

- 定向检查共 80 项：道具、伤害/防御耦合、AI 选择、教学连续流程、掉落、资源和联机模块；没有运行全项目测试套件。
- 新规则覆盖恩惠双方各自回血/上限/零值，破坏绝对差两种方向/零伤害/和平/不耗盾/不受酒和指虎影响，强欲在有无坚韧时重置双手及空过豁免，删除道具和旧存档拒绝。
- 浏览器实测教学两只目标手引导框宽高完全相同；上方目标手可点击，后续攻防与真实增幅补给正常。
- 实测增幅约 0.9 秒到达目标并改变数字，信息卡继续停留阅读；碎片手雷造成 15 HP 伤害，播报“护盾被消耗，格挡一半伤害”，不再显示额外数字重置文本。
- 回合动画中打开配方详情，动画结束及多次状态刷新后仍保留；展开规则细节也保留。规则文字提高对比度，展开/窗口变化时自动重新定位。
- 1280×720 底栏切页前后高度均为 120 px、顶部均为 600 px；844×390 横屏均为 80 px、顶部 310 px，页面无纵向溢出。1920×1080 全屏 13 种道具全部可见且无需横向滚动。
- 服务器导入相同规则与演出时长；网络定向测试通过旧客户端拒绝、旧对局回准备、计时、重复请求、断线重连和同房再战。

## 固定计时框与回合交接 · 2026-09-13

- 35 项定向检查通过：联机模块、回合演出时序、教学、补给快照、记录和 HUD；未运行全量测试。构建成功。
- 浏览器实际采样：11 秒白底，10 至 6 秒黄底，5 秒及以下黄底且亮度持续变化；普通行动时箭头隐藏。手动和超时交接保持计时框尺寸，框内显示粗箭头，交接后恢复 30 秒。
- 实际完成教学连续攻防，任意目标手可点；增幅、护盾图鉴、手雷结算正常。新总结包含指定的两处换行；640×360 下继续按钮底部 276 px，底栏从 280 px 开始，无遮挡或页面溢出。
- 首次归一获得状态后，玩家卡高度保持 112 px，状态详情可点击；844×390、640×360 横屏图标在卡片外侧，玩家卡高度 65 px。最后特殊胜利演示按钮完整显示。
- 1920×1080 实查底部工具、配方和白底大倒计时；空道具位文字为空，浏览器控制台无错误。联机仍采用规则 15 / 协议 2。

## 播报队列与归一衔接 · 2026-09-13

- 43 项定向检查通过，覆盖手部动画、详情、技能演出、伤害/防御、教学与网络；未运行全项目测试套件。
- 本地代表性联机局面实际连续使用酒、回响、镜像，640×360 下显示一条、其余排队，后续均能显示并清空；操作区底部最高 265 px，底栏从 280 px 开始。未显示播报区滚动条，控制台无错误。
- 复现并修复窄横屏确认使用被中部按钮覆盖；实际命中确认按钮，连续道具结算成功。状态图标采用两列，四个状态无滚动条。
- 四技能图鉴在 640×360 下内容高度与滚动高度相同，完整展示；1920×1080 实查归一新文案、无配方规则折叠、永久计数说明和中性灰阴影。
- 第二次归一按约 180 ms 间隔采样，从太极覆盖直接切到胜利弹窗，两者之间没有出现战场裸露帧。测试局面和延长的操作时间仅存在于本地测试脚本，不进入部署包；正式计时保持 30 秒。

## 过期内容清理 · 2026-09-14

- README、规则、网络、部署、美术与试玩说明统一为现行规则 15 / 协议 2，删除混杂的旧版设计和发布说明。保留当前规则验证记录；更早记录通过 Git 历史追溯。
- 删除 52 组无对应界面的旧胜利页、框外箭头、回合横幅和归一旧光环样式，以及无调用的反馈接口。旧胜利背景与来源条目一起移除；字体许可证、回退手势、现行素材和旧版本拒绝测试保留。
- 47 项定向测试通过，覆盖共享规则、AI、教学、表现与真实双端网络；构建、发布包和素材来源清单检查通过。
- 浏览器实际完成两节教学，验证增幅、手雷、回合交接、两次太极归一和完成教学页，控制台无错误。本轮不改变玩法、协议或存档格式。

## PVE 结算难度 · 2026-09-15

- PVE 结算页从本局 session 读取对手难度，仅人机模式显示；玩法、协议和存档格式不变。
- 本地测试局面通过正常菜单选择大师，实际合成剪刀获胜，结算显示“对手难度：大师”；再来一局再次获胜仍显示大师。测试局面不进入发布包。
- 实查 1280×720 和 844×390 横屏结算页，难度、比分和三个按钮完整显示，控制台无错误；生产构建通过。

## 菜单入口与全屏提示 · 2026-09-15

- 新手教学移至开始游戏后的模式选择页首项，与本地、联机并列；浏览器实点确认可以进入教学介绍。
- 首次菜单就绪后展示 F11 全屏浮窗，5 秒后淡出移除；实查提示位置、自动移除和 F11 立即关闭，控制台无错误。生产构建通过。
- 仅更新静态菜单文件，不改变联机服务、规则或存档，不重启服务。

## PVE 三档升级 · 2026-09-15

- 15 项 AI 定向检查通过：现成斩杀、酒接狙击、破坏斩杀、拒绝无效回血、第二次归一多道具路线、拆解对手下回合归一、碰零避罚、合法性、状态不变性与不依赖真实 RNG。未运行无关全量套件。
- 与提交 0f3d132 的旧大师进行 16 组种子（7919 × 1～16）、每组交换先后手，共 32 局。新版大师 28 胜 4 负，无超过 500 操作的未结束对局。决策最慢约 202 ms，机器与浏览器性能可能不同；此结果不代表真人胜率。
- 浏览器从正式菜单进入大师 PVE，实际完成计算、自动回合结束、AI 计算并交回玩家；控制台无错误。构建通过。
- 本次仅更新 AI、后台计算降级流程与文档，不改变规则 15 / 协议 2。通过静态资源更新上线，不重启联机服务。

## 移除制衡与提示强化 · 2026-09-15

- 制衡从目录、结算、说明、实时预览、AI、演出目标选择与静态图标中移除，现为 12 种道具、34 张墨线图标。其余道具权重不变，总权重 28。
- 58 项定向检查通过，覆盖抽取、道具、图鉴、教学、AI、网络及旧版拒绝。规则升级为 16、协议仍为 2；旧 PVE 存档拒绝，旧联机客户端提示刷新。
- 浏览器实际查看道具大全只列 12 项，回合红蓝柔光正常切换；当时验证过开发者介绍中的幻灯片入口；该内部截图及入口现已移除。
- 底光混合强度从 0.3 提至 0.4，降级模式同步加强透明度，范围与移动过渡保持。生产构建通过。

## 无道具模式与设置更新

覆盖默认道具开启、10/30/60 秒校验、花蝶扇 20 与双枪四发各 10、无道具获取与技能补牌禁用、三档 AI 剪刀低优先级及斩杀、存档与回放设置保留。真实 WebSocket 双客户端验证设置一致、计算结果一致和断线恢复；规则升级后旧房间回到准备状态。浏览器检查 PVE 默认设置与无道具战场、倒计时和 AI 回合交接。自动对战只验证执行和规则，不代表真人胜率；无道具防守对局可能较长。

## 四档人机 · 2026-09-16

- 简单、进阶、高手、大师同步接入设置、局内标签、结果页和本地存档。新增 expert；大师保留当前正式策略。此前实验目录未接入运行时。
- 有道具验证采用 801～812 × 7919 的 12 个种子，每组交换先后手，三组各 24 局：进阶对简单 21:3；高手对进阶 16:8；大师对高手 19:5。共 72 局均在 400 操作内结束，大师相关组单次决策最高约 221 ms。本机模拟不包含动画与真实倒计时，不代表真人胜率。
- 无道具采用 51～54 四个种子的合法随机开局前缀，每组交换先后手，三组各 8 局：进阶对简单 4 胜 0 负 4 未决；高手对进阶 0 胜 0 负 8 未决；大师对高手 0 胜 0 负 8 未决。未决表示达到 400 操作上限，界面并无平局规则。检查仍在合法计算、数字继续变化，属于策略僵持，非程序卡死；不宣称该模式的梯度已由胜率证实。
- 54 项定向检查通过，涵盖四档合法性、可见斩杀、空过避罚、旧行为约束、归一、无道具技能、存档恢复、难度按钮与不读取真实 RNG。另有 24 个人工局面确认大师与原版一致，12 个有道具局面确认高手与原进阶一致。
- 浏览器实际选择高手并开局，四个难度完整显示；生产构建通过。已通过 RUN Platform API 增量上线，59 个线上构建文件与本地逐字节一致；健康检查规则 17 / 协议 2 正常。保留旧构建资源，未重启联机服务。

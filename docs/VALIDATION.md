# 当前验证 · 信息与演出打磨 · 规则 12 / 协议 2

- 按要求未跑全量测试。22 项相关检查通过：效果文案、实际受影响方、致命伤不虚报后续效果、护盾解释、历史去重、教学流程及既有演出时序。
- 生产构建通过；浏览器实看桌面剪刀的受击方数字变化、手机手雷护盾减伤与消散、五节教学及新九九归一演出。技能图标不再遮住说明，主要数字保持可读；历史入口显示最新记录及阅读顺序。
- 本地真实双 WebSocket 验证未计算结束被拒绝、动画暂停计时、30 秒换人、身份重连、合成自动释放后归 [1]，双端快照一致，测试房间已清理。
- 线上验收：202 个部署文件与本地一致；68 个发布文件经 HTTPS SHA-256 逐一一致。真实双端联机同样通过计算、换人、动画计时、重连与自动释放，测试房间已清理；线上教学入口正常，无控制台错误。备份：`backups/readability-20260911-232316`。
- 本轮不改玩法数值或存档结构；规则 12 存档继续兼容。未进行全量回归、弱网压力测试、真人平衡测试或真实扬声器听感评估。

# 历史验证 · 2026-09-11 · 规则 12

- 143 项测试通过：手动结束的计算门槛、无计算目标例外、技能后仍需计算、三档 AI 候选合法、服务器拒绝伪造超时、自动超时与存档回放，以及既有玩法和联机回归。
- 浏览器实测教学入口按钮、ESC 暂停、动画中暂停再继续：暂停时数字不变，继续后只结算一次；图鉴查看门槛保留。
- 桌面与 390×844 手机实测计算前禁用、计算后有道具也金色高亮，手机无横向溢出。真实双 WebSocket 本地与线上检查拒绝未计算结束、计算、合成保留数字、自动攻击后归 [1]、同状态快照、30 秒计时与身份重连；测试房间已清理。
- 结束回合分为未计算禁用、计算后金色提示、无其他操作时加强动态三个状态；允许操作后才显示联机合成面板。
- 线上部署为规则 12 / 协议 2，程序与房间数据已备份；200 个部署文件与本地逐一校验一致，68 个发布文件经 HTTPS 哈希核验一致；线上手机教学入口与 ESC 暂停实测通过，无控制台错误。
- 无新增音效或特效资源，沿用上一轮分段 HP、受击击退、护盾消散、4.4 秒排队提示；未进行真人长期试玩或真实扬声器听感评估。

# 历史验证 · 2026-09-11 · 规则 11

- 140 项自动测试通过。覆盖全部技能合成保留数字、攻击后归 [1]、两种胜利条件、图鉴解锁教学、[8] + [1] 凑九、对手回合外 [0] + [0]、切换与技能动画暂停计时；保留既有 AI、重连、去重与兼容性测试。
- 浏览器实际完成教学计算、图鉴解锁、增幅、剪刀、护盾；全部五节教学已完成，包含 HP 清零与九九归一；桌面和 390×844 手机尺寸均检查。确认未查看图鉴时结束回合禁用、查看后高亮；浮窗 z-index 230，高于教学遮罩 110。手机无横向溢出，缩短道具区域；控制台未见运行错误。
- 真实双 WebSocket 本地与线上验证：建房、准备、重连、合成保留 [2] + [2]、服务器自动释放剪刀、对手变为 [0] + [0]、新回合恢复 30 秒，双方快照一致。测试房间已清理。
- 浏览器分时读取确认：剪刀扣血至 94 时己方仍为 [2] / [2]，之后才归 [1]；护盾教学显示护盾消散、HP 从 99 滚动至 84，无伤害斜线。
- 生产构建通过。未进行真实扬声器听感、新一轮真人平衡或弱网压力测试；本轮不修改音效资源或 AI 策略。

- 线上 `/health` 为规则 11 / 协议 2，199 个部署文件与本地 SHA-256 一致，68 个发布文件经 HTTPS 逐一校验一致；Supervisor 后台运行。

# 历史验证 · 2026-09-11 · 规则 10

- 136 项自动测试全部通过，生产构建通过。规则 10 增加镜像×回响的双方手数字、演出写入与道具先后顺序测试；修复先行动再用强欲/肾上腺素误获坚韧。覆盖三档 AI 合法指令、不可窥探真实随机流、酒接狙击斩杀、存档难度、旧存档拒绝和事件差值。
- 30 个固定种子、交换先后手：进阶对简单 60:0，大师对进阶 50:10，均无 600 步内未结束的对局。此为 AI 对战基准，不等同真人胜率；可运行 `node scripts/benchmark-ai.mjs` 重现。
- 浏览器实测难度选择、大师自动对战、教学道具离槽与技能释放；1280×720、390×844 下横幅、血条、前后数字与图鉴可见。实际发现并修复 shader 名称冲突，当前构建正常编译渲染。
- PVE 浏览器恢复检查：简单难度保留，剩余 9 秒保存后恢复仍为 9 秒。
- 音效事件族及静音节点测试通过；未进行真实扬声器听感、真人难度标定、高并发和弱网压测。

以下为历史版本记录。

## 历史验证 · 2026-09-11 · 规则 9

- 128 项测试全部通过：100 局 AI 对战、真实双 WebSocket 完整对局、统一回合道具/合成/计算交错、每回合一次计算、超时与重复请求、重连和旧房间迁移。
- 计时测试检查同回合逐毫秒保留剩余时间，动画暂停，换人重新 30 秒；超时不会替玩家使用道具。
- 计算完成后禁用仅影响本回合计算的镜像/回响，保留道具到下回合；无合法操作时提示结束。
- 浏览器实际检查桌面 1280×720 和窄屏 390×844：顶部回合归属、大号倒计时及相邻结束按钮，选手后动态提示，计算后继续选择道具，道具槽与手部目标，底部图鉴滚动。
- 教学实际操作覆盖加和、新增组合说明及结束回合；全部五章的命令流程通过真实规则测试。浏览器无控制台错误，中文显示正常。
- 生产构建通过。Three.js 演出分包约 554 KB，保留现有体积提示；未进行真人平衡、高并发或真实手机弱网测试。

以下为历史版本验证记录，旧阶段描述不代表当前规则。

## 历史验证记录 · 2026-09-09 · 规则 6

- 106 项自动测试通过，包含 100 局 AI 对战、真实双 WebSocket 完整对局、重连和旧规则房间迁移。
- 验证酒只增强首段、叠层消耗与保留条件；肾上腺素的生效期、所有伤害来源、叠加虚弱和指虎、护盾次序、窃取、归一保留增益。
- 联机测试验证酒的重复请求不重复使用，空道具阶段 1 秒自动推进，重连快照一致。
- 实际浏览器验证：两阶段显示；教学无道具及用尽道具自动进入行动；手部目标高亮并直接使用；真实算式红蓝着色；历史简洁显示回合和行动；肾上腺素槽位旁确认后消耗、获得 3 回合状态并交接对手。
- 浏览器发现并修复确认按钮被通用按压动画移动导致点击落空；已重新实点确认生效。顶部取消按钮对比度已修正。
- 36 张图标资源完整，生产构建通过，无浏览器控制台错误。新道具图标为黑白透明 WebP。

本轮部署验证结果见文末。真人平衡、手机触屏、公网弱网与高并发未全面验证。Three.js 分包约 529 KB，构建有体积提示。

## 界面精简与联机复核

- 移除战场人物与红蓝底图；顶栏缩减为模式、两阶段、计时和当前操作。
- 手势按视口比例排布，数字朝内，避免与两侧道具槽重叠；保留现有 WebGL 接触、墨线及技能演出时长。
- 道具说明补齐生效期限、背包上限、制衡消耗顺序与破坏穿盾；顶部只显示下一步，回响/镜像提示实际改变的手。
- 修复联机阶段切换瞬间显示旧倒计时；等待确认期间明确提示，不重复提交。
- 双浏览器房间实测：不同中文昵称、准备、倍增使用、双方触碰、22 合成剪刀并攻击；无控制台错误。

部署验收：RUN 服务已后台重启并返回 rulesVersion 6 / protocolVersion 2；HTTPS 首页与本地构建一致。线上双浏览器创建/加入房间、保存不同中文姓名、准备开局和首轮触碰通过。部署前已备份，保留 data 与 supervisor 配置。

## 规则 7 本地验收

- 112 项测试通过：新增连续两次跳过、正常行动打断、坚韧五回合期限、清除剩余 skip、强欲/肾上腺素与坚韧、敌我跳过免疫、窃取、放弃合成结束、封印单手上限及不消耗非法目标。
- 服务器测试确认 forge 后 deadlineAt=readyAt，tick 自动发动一次技能，无需客户端二次请求。
- 浏览器走通全部三节教学，剪刀及第二枚九均自动释放；空白点击保留选手。双浏览器联机准备、计算凑成 22、选择剪刀后自动攻击、扣血与换回合同步，刷新恢复正常。
- 发现并修复非行动阶段快速点击手势误开图鉴；查询保留在底部图鉴。技能选择布局、手部热区、色流与聚光已检查。
- 新增坚韧图标；五指护盾改为浅面、单轮廓与轻微呼吸。此轮未部署，线上仍是规则 6，供这一批反馈本地验收。

## 规则 7 第二批界面验收

- 首屏直接呈现封面；主菜单三项操作，联机等待和房间面板沿用封面遮罩。移除战斗双层顶部栏，改为单行回合、计时、阶段及操作提示。
- 道具获得飞向实际槽位并短暂高亮；可用道具弱呼吸；槽位下固定「进入行动」，不可操作时禁用。道具使用结果放在战场上方。
- 双手满足配方时数字与手部光圈加强，底部对应配方亮起；单手匹配弱提示，悬停查询完整组合。
- 昵称限 10 个可见字符，保留完整 emoji 和组合字符；仅本人名称可点击编辑并标记（我）。服务端同步截短规则，读取旧数据时同步规范化。
- 技能全屏遮住战场和图鉴，伤害/真实伤害/附加效果分层显示；结尾统计实际段数与总扣血，致死后未发动的段数和回合持续伤害不计入。归一直接进入大演出。
- 114 项自动测试通过，生产构建与 37 张图标发布包校验通过。浏览器完成三节教学、剪刀与蓝方九九归一全屏演出；双端创建加入房间、十字截短、对战内 emoji 改名同步、进入行动、触碰换回合、刷新恢复通过，未发现控制台错误。
- 此批继续保留本地预览：http://127.0.0.1:3006/ 。尚未更新线上。

## 视觉与声音本地验收
- 116 项测试通过；新增音量默认值、独立静音、持久化、后台暂停，以及记录按回合聚合和姓名转义验证。开局服务端保护窗口更新为 1400ms，测试确认之后仍有完整道具操作时间。
- 浏览器验证本地与双端联机姓名对抗入场、本人标识、菜单收纳、对局记录、联机离开后的胜利界面（无比分）。WebGL 网点背景正常，无浏览器控制台错误。
- BGM 解码为 137.69 秒，实际播放与循环标志正常；音乐调为 0 后暂停，音效仍为 50%，刷新后保留设置。验收后恢复音乐 50%。
- 合成练习发现并修正纸色卡面上的浅色文字；道具到位前隐藏槽内内容。构建与发布包校验通过；此轮未部署。

## 2026-09-09 发布前验收
- 117 项测试通过：规则 7、连续跳过保护、封印上限、技能自动释放、数值与道具交互、双端同步、重连、重复请求、开局计时保护、昵称、声音设置与记录聚合。
- 删除按钮装饰箭头，保留计算方向符号；删除设置备注，关键提示加拼贴纸底。胜利页增加低对比网点与大字背景，背景人物缓慢漂移，减少动态模式禁用。
- 对局记录按回合和阶段分组，压缩措辞，连续同等伤害合并为总伤害/段数，不合并不同护盾结算。规则详情修正强欲与坚韧交互说明。
- RUN 发布将沿用原域名及数据目录；规则 6 的旧对局回到待准备，保留房间与昵称。保留部署前快照供回滚。

上线验收完成：正式域名 health 返回 rulesVersion 7 / protocolVersion 2，首页与本地构建一致，音乐返回 HTTP 200。线上双端实测独立昵称、创建/加入、准备开局、破坏扣至 97 HP、触碰 1+1→2、换回合及离开判负通过，无浏览器控制台错误。胜利页发现装饰背景溢出，已限制裁切并保留矮屏纵向滚动。发布前已备份线上 dist/src/server/data，GitHub 同步本次版本。

## 规则 8 发布验收

- 124 项自动测试通过，100 局 AI 对战均结束（最长 141 回合）；双 WebSocket 完整对战状态一致。
- 新规则覆盖一次计算与多次合成、放弃数值记忆、认真一拳立即结束及坚韧交互、技能后完整 20 秒计时。
- 四节教学在浏览器走通，护盾实际将手雷 30 减至 15，技能后继续计算，对手两只目标手均可操作。
- PVE 跨刷新继续，昵称与随机进度回放一致；房间凭证在新标签恢复原席位，仅房间号的其他用户无法接管。
- 实际渲染复查菜单入场、存档按钮和联机大厅，修复窄窗口断字、横向溢出和删除旧说明入口后的残留引用。

## 菜单、教学与道具时序修订

- 首屏实际采样四个按钮透明度均为 0，旧 CSS 动画和背后重复菜单已停用；入场结束后再开放点击。
- 五节教学在浏览器完整走通；贴纸确认后操作，护盾减伤、HP 归零与九九归一结果正常。
- 道具横幅 1.6 秒，加 0.18 秒停顿；横幅结束后才进入后续选择，客户端和服务端时序一致。
- 新 BGM 为 pixel.mp3，137.221 秒，浏览器确认播放和循环；旧 BGM 已移除，默认音量与独立设置保留。

## 菜单与 ESC 返回修订

- 修复实测复现的连续 ESC 导致原生 dialog 关闭、菜单背景仍遮挡且无法操作的问题；弹窗内统一处理 ESC，主菜单停留，模式菜单逐级返回。
- 浏览器验证设置、开始游戏、本地模式、联机入口连续返回后仍可再次进入；首次加载显示 F11 提示，按钮完成入场后开放。
- 主菜单、联机与普通弹窗按钮默认深色，悬停或键盘聚焦时高亮；开发者说位于第四项，四项按钮依次缩小。
- 125 项自动测试通过，玩法与联机规则版本未变化。

2026-09-11 上线实测：HTTPS `/health` 返回规则 9 / 协议 2，191 个部署文件与本地 SHA-256 一致；真实双 WebSocket 建房、准备、计算后保持当前回合、动画保留剩余时间、结束后新回合 30 秒及身份重连均通过，测试房间已退出清理。发布前程序与房间备份位于服务器 `backups/turn-v9-20260911-204830`。

规则 10 上线验收：136 项测试通过；198 个部署文件与本地 SHA-256 一致，68 个 HTTPS 发布文件逐一一致。正式域名规则 10 / 协议 2，真实双 WebSocket 的同步、动画计时、结束回合、重连与清理通过。程序与房间数据备份：`backups/turn-v10-20260911-211651`。
